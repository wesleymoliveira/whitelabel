import { Box, styled } from '@mui/material';

export const Wrapper = styled(Box)`
	border-radius: 30px;
	overflow: hidden;
`;

export const Video = styled('video')`
	border-radius: 30px;
	width: 100%;
	height: 100%;
`;
